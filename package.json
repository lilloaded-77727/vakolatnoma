# ============ PACKAGE.JSON NI TO'G'IRLASH ============

Remove-Item package.json -Force -ErrorAction SilentlyContinue

@"
{
  "name": "vakolatnoma",
  "version": "1.0.0",
  "description": "O'zbekiston vakolatnoma tizimi",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5"
  }
}
"@ | Out-File -FilePath "package.json" -Encoding UTF8

Write-Host "✅ package.json to'g'irlandi!" -ForegroundColor Green
